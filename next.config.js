module.exports = {
  reactStrictMode: true,
	i18n: {
    locales: ["en"],
    defaultLocale: "en",
  },
	/*
	async rewrites() {
    return [
      // Rewrite everything else to use `pages/index`
      {
        source: '/:path*',
        destination: '/books-joshua',
      },
    ];
  },
	*/
}
